import 'package:flutter/painting.dart';

import 'colors.dart';

const dropdownShadow = BoxShadow(
  color: DROPDOWN_SHADOW_COLOR,
  blurRadius: 24.0,
  offset: Offset(0, -8),
);

const imageContainerShadow = BoxShadow(
  color: DROPDOWN_SHADOW_COLOR,
  blurRadius: 24.0,
  offset: Offset(0, 8),
);
